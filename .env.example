# ZJ Panel production environment
NODE_ENV=production
PORT=6767
JWT_SECRET=replace-with-a-long-random-secret
DOCKER_SOCKET_PATH=/var/run/docker.sock
VITE_ENABLE_DEVELOPER_PANEL=false
# Optional: Gemini AI key (server-side only)
# GEMINI_API_KEY=
